import { FooterTypes } from './footer';
import { navbar } from './navbar';
import { Theme } from './theme';

export const TYPES = {
  ...FooterTypes,
  ...navbar,
  ...Theme,
};

export default TYPES;
